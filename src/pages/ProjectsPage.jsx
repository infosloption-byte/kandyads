import React from 'react';
import { useState } from 'react';
import { motion } from 'framer-motion';
import { ArrowUpRight } from 'lucide-react';
import { portfolio } from '../site-data';
import { Site, SectionHero, Reveal, SectionTag, PrimaryCTA } from '../site-shell';
const cats=['All','Signage','Vehicle Branding','Outdoor Advertising','Retail Branding','Print & Displays','Events','Displays','Promotional'];
export default function ProjectsPage(){const [cat,setCat]=useState('All');const filtered=cat==='All'?portfolio:portfolio.filter(p=>p[1]===cat);return <Site><SectionHero kicker="PROJECTS" title="Proof is the portfolio." text="Explore real Kandy Ads work across signage, retail branding, vehicles, outdoor advertising, printing and events."/><section className="container project-intro"><div><SectionTag>SELECTED WORK</SectionTag><h2>Built for the places people actually see.</h2></div><p>Use the filters to browse the capabilities represented in our supplied project portfolio. Each project can later become a full case study with scope, materials, location and results.</p></section><div className="container filters">{cats.map(c=><button className={cat===c?'active':''} onClick={()=>setCat(c)} key={c}>{c}</button>)}</div><div className="container project-grid">{filtered.map((p,i)=><Reveal key={p[0]} delay={Math.min(i*.02,.12)}><motion.article className="project-card" layout whileHover={{y:-4}}><img src={'/src/assets/'+p[2]} alt={p[0]} loading="lazy"/><div><span>{p[1]}</span><h3>{p[0]}</h3><p>Selected portfolio reference</p><ArrowUpRight className="project-card-arrow"/></div></motion.article></Reveal>)}</div><section className="container project-note"><strong>Want to see a similar solution for your business?</strong><PrimaryCTA>Get a Quote</PrimaryCTA></section></Site>}
