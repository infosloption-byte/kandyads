import React from 'react';
import { Plus, RefreshCw, Search, ArrowRight } from 'lucide-react';
import { api } from '../../../api';
import ResourceTable from '../../shared/components/ResourceTable';
import Modal from '../../../components/common/Modal';
import EnquiryForm from '../components/EnquiryForm';
import EnquiryQuoteForm from '../components/EnquiryQuoteForm';

export default function EnquiriesPage(){
 const [search,setSearch]=React.useState('');const [state,setState]=React.useState({loading:true,error:'',items:[],total:0});const [open,setOpen]=React.useState(false);const [quoteEnquiry,setQuoteEnquiry]=React.useState(null);const [saving,setSaving]=React.useState(false);const [notice,setNotice]=React.useState('');
 const load=React.useCallback(async()=>{setState(s=>({...s,loading:true,error:''}));try{const r=await api.listEnquiries({search:search||undefined});setState({loading:false,error:'',items:r.data||[],total:r.meta?.total||0})}catch(e){setState({loading:false,error:e.message||'Unable to load enquiries',items:[],total:0})}},[search]);
 React.useEffect(()=>{const t=setTimeout(load,250);return()=>clearTimeout(t)},[load]);
 async function create(input){setSaving(true);try{await api.createEnquiry(input);setOpen(false);await load()}finally{setSaving(false)}}
 async function createQuote(input){setSaving(true);setNotice('');try{const result=await api.convertEnquiryToQuote(quoteEnquiry.id,input);setQuoteEnquiry(null);setNotice(`${quoteEnquiry.number} converted to ${result.data.number}.`);await load()}catch(e){setNotice(e.message||'Unable to create quote.')}finally{setSaving(false)}}
 return <div><div className="page-head"><div><p className="eyebrow">SALES & CRM</p><h1>Enquiries</h1><p>Track incoming requirements and move qualified work into quotations.</p></div><button className="primary" onClick={()=>setOpen(true)}><Plus size={16}/> New Enquiry</button></div>{notice&&<div className="panel" style={{marginBottom:16}}>{notice}</div>}<section className="panel table-panel"><div className="table-toolbar"><label className="search-box"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search enquiries…"/></label><button className="icon-button" onClick={load} aria-label="Refresh enquiries"><RefreshCw size={16}/></button><span className="table-count">{state.total} enquiries</span></div><ResourceTable loading={state.loading} error={state.error} rows={state.items} columns={[{key:'number',label:'Number'},{key:'client',label:'Client',render:r=><strong>{r.client?.companyName||'—'}</strong>},{key:'requirement',label:'Requirement'},{key:'siteLocation',label:'Site'},{key:'status',label:'Status',render:r=><span className="table-status">{r.status.replaceAll('_',' ')}</span>},{key:'targetDate',label:'Target',render:r=>r.targetDate?new Date(r.targetDate).toLocaleDateString():'—'},{key:'actions',label:'Actions',render:r=>!r.quote&&['OPEN','QUOTING'].includes(r.status)?<button className="secondary" onClick={()=>setQuoteEnquiry(r)}><ArrowRight size={14}/> Create Quote</button>:r.quote?<span className="table-status">{r.quote.number}</span>:'—'}]} /></section><Modal open={open} onClose={()=>setOpen(false)} title="New enquiry" description="Capture the client requirement before preparing a quotation." width="760px"><EnquiryForm onSubmit={create} onCancel={()=>setOpen(false)} submitting={saving}/></Modal><Modal open={!!quoteEnquiry} onClose={()=>setQuoteEnquiry(null)} title="Create quote from enquiry" description={quoteEnquiry?`${quoteEnquiry.number} · ${quoteEnquiry.client?.companyName||''}`:''} width="820px">{quoteEnquiry&&<EnquiryQuoteForm enquiry={quoteEnquiry} onSubmit={createQuote} onCancel={()=>setQuoteEnquiry(null)} submitting={saving}/>}</Modal></div>;
}
