import React from 'react';
import { Plus, RefreshCw, Search } from 'lucide-react';
import { api } from '../../../api';
import ResourceTable from '../../shared/components/ResourceTable';

export default function EnquiriesPage(){const [search,setSearch]=React.useState('');const [state,setState]=React.useState({loading:true,error:'',items:[],total:0});const load=React.useCallback(async()=>{setState(s=>({...s,loading:true,error:''}));try{const r=await api.listEnquiries({search:search||undefined});setState({loading:false,error:'',items:r.data||[],total:r.meta?.total||0})}catch(e){setState({loading:false,error:e.message||'Unable to load enquiries',items:[],total:0})}},[search]);React.useEffect(()=>{const t=setTimeout(load,250);return()=>clearTimeout(t)},[load]);return <div><div className="page-head"><div><p className="eyebrow">SALES & CRM</p><h1>Enquiries</h1><p>Track incoming requirements and move qualified work into quotations.</p></div><button className="primary"><Plus size={16}/> New Enquiry</button></div><section className="panel table-panel"><div className="table-toolbar"><label className="search-box"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search enquiries…"/></label><button className="icon-button" onClick={load} aria-label="Refresh enquiries"><RefreshCw size={16}/></button><span className="table-count">{state.total} enquiries</span></div><ResourceTable loading={state.loading} error={state.error} rows={state.items} columns={[{key:'number',label:'Number'},{key:'client',label:'Client',render:r=><strong>{r.client?.companyName||'—'}</strong>},{key:'requirement',label:'Requirement'},{key:'siteLocation',label:'Site'},{key:'status',label:'Status',render:r=><span className="table-status">{r.status}</span>},{key:'targetDate',label:'Target',render:r=>r.targetDate?new Date(r.targetDate).toLocaleDateString():'—'}]} /></section></div>}
