import React from 'react';
import { Plus, RefreshCw, Search } from 'lucide-react';
import { api } from '../../../api';
import ResourceTable from '../../shared/components/ResourceTable';

export default function QuotesPage(){const [search,setSearch]=React.useState('');const [state,setState]=React.useState({loading:true,error:'',items:[],total:0});const load=React.useCallback(async()=>{setState(s=>({...s,loading:true,error:''}));try{const r=await api.listQuotes({search:search||undefined});setState({loading:false,error:'',items:r.data||[],total:r.meta?.total||0})}catch(e){setState({loading:false,error:e.message||'Unable to load quotes',items:[],total:0})}},[search]);React.useEffect(()=>{const t=setTimeout(load,250);return()=>clearTimeout(t)},[load]);return <div><div className="page-head"><div><p className="eyebrow">SALES & CRM</p><h1>Quotes</h1><p>Prepare estimates, compare expected delivery costs and track client decisions.</p></div><button className="primary"><Plus size={16}/> New Quote</button></div><section className="panel table-panel"><div className="table-toolbar"><label className="search-box"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search quotes…"/></label><button className="icon-button" onClick={load} aria-label="Refresh quotes"><RefreshCw size={16}/></button><span className="table-count">{state.total} quotes</span></div><ResourceTable loading={state.loading} error={state.error} rows={state.items} columns={[{key:'number',label:'Quote'},{key:'client',label:'Client',render:r=><strong>{r.client?.companyName||'—'}</strong>},{key:'status',label:'Status',render:r=><span className="table-status">{r.status}</span>},{key:'total',label:'Total',render:r=>`LKR ${Number(r.total||0).toLocaleString()}`},{key:'expectedMargin',label:'Expected Margin',render:r=>`LKR ${Number(r.expectedMargin||0).toLocaleString()}`},{key:'validUntil',label:'Valid Until',render:r=>r.validUntil?new Date(r.validUntil).toLocaleDateString():'—'}]} /></section></div>}
