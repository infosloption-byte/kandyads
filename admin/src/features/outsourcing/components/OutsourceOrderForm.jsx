import React from 'react';
const initial={number:'',jobId:'',vendorId:'',scope:'',specification:'',quantity:'',dueDate:'',agreedCost:'',status:'REQUESTED'};
export default function OutsourceOrderForm({jobs,vendors,onSubmit,submitting}){const [form,setForm]=React.useState(initial);const set=(k,v)=>setForm(f=>({...f,[k]:v}));const submit=e=>{e.preventDefault();onSubmit({...form,jobId:Number(form.jobId),vendorId:Number(form.vendorId),quantity:form.quantity===''?null:Number(form.quantity),agreedCost:Number(form.agreedCost),dueDate:form.dueDate||null})};return <form className="entity-form" onSubmit={submit}><div className="form-grid two"><label>Order number<input value={form.number} onChange={e=>set('number',e.target.value)} placeholder="OUT-2026-001" required/></label><label>Status<select value={form.status} onChange={e=>set('status',e.target.value)}>{['REQUESTED','APPROVED','SENT','IN_PROGRESS','RECEIVED','REJECTED','CANCELLED'].map(s=><option key={s}>{s}</option>)}</select></label><label>Job<select value={form.jobId} onChange={e=>set('jobId',e.target.value)} required><option value="">Select job</option>{jobs.map(j=><option key={j.id} value={j.id}>{j.number} · {j.title}</option>)}</select></label><label>Vendor<select value={form.vendorId} onChange={e=>set('vendorId',e.target.value)} required><option value="">Select vendor</option>{vendors.map(v=><option key={v.id} value={v.id}>{v.code} · {v.companyName}</option>)}</select></label><label>Quantity<input type="number" min="0" step="0.001" value={form.quantity} onChange={e=>set('quantity',e.target.value)}/></label><label>Agreed cost (LKR)<input type="number" min="0" step="0.01" value={form.agreedCost} onChange={e=>set('agreedCost',e.target.value)} required/></label><label>Due date<input type="date" value={form.dueDate} onChange={e=>set('dueDate',e.target.value)}/></label></div><label>Scope<textarea rows="3" value={form.scope} onChange={e=>set('scope',e.target.value)} required placeholder="Describe the outsourced production scope."/></label><label>Specification<textarea rows="3" value={form.specification} onChange={e=>set('specification',e.target.value)} placeholder="Materials, dimensions, finish, quality requirements..."/></label><button className="primary" disabled={submitting}>{submitting?'Saving…':'Create outsource order'}</button></form>}
