# Kandy Ads Public Website

This directory is the target home for the existing React/Vite public website.

## Current status
The current production website is still at the repository root while the application is being migrated. Do not move or delete the existing root files until the frontend migration task in `TASKS.md` is executed and verified.

## Target
- React + Vite
- Public marketing website
- Routes: `/`, `/about`, `/services`, `/projects`, `/industries`, `/process`, `/contact`, `/quote`
